#!/bin/sh
df -h -x tmpfs -x devtmpfs | awk '!/^Filesystem|\/boot$/{
    if ($6 != "/") {
        split($6, a, "/")
        mp = a[length(a)]
    } else {
        mp = $6
    }
    printf("[%s %s] ", mp, $4)}
' | sed 's/^/ /;'
echo
