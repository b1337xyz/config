#!/bin/sh

main() {
    pacman -Qqu --color=never
}
case "$BLOCK_BUTTON" in
    1|2)
        notify-send "Upgradable packages" "$(main)"
    ;;
esac

n=$(main | wc -l)
[ "$n" -gt 0 ] && printf '%s <span size="13pt"><tt></tt></span>\n' "$n"

exit 0
