set $exec exec --no-startup-id

$exec alacritty -e tmux
$exec qbittorrent
$exec tmux new-session -d -s cmus cmus
$exec unclutter --idle 1 --jitter 2 --root
$exec ~/.cache/xwallpaper
$exec set_screen.sh

# vim: ft=i3config
